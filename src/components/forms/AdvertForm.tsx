import { Typography } from "@mui/material";
type Props = {
    submitFn: any
}
const AdvertForm: React.FC<Props> = ({submitFn}) =>
<Typography variant="h3">Advert Form</Typography>
export default AdvertForm;